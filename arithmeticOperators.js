let a = 15;
let b = 5;
let c;
c = a + b; // 20
c = a - b; // 10
c = a * b; // 75
c = a / b; // 3
c = a % b; // 0
c = a ** b; //15^5 = 759375

console.log(5 ? 4 : 10);
console.log(42 == "42" ? 4 : 10); // 4 could be a command if true 10 **command  if false;
if (5) {
    // do something
    return 4:
} else {
    // not true do something else
    return 10
}

